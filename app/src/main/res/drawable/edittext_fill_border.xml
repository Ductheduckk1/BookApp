<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Màu nền -->
    <solid android:color="@color/backgrount_button" />

    <!-- Viền (độ dày & màu) -->
    <stroke
        android:width="1dp"
        android:color="@color/border_buton" />

    <!-- Bo góc -->
    <corners android:radius="8dp" />

    <!-- Khoảng đệm bên trong -->
    <padding
        android:bottom="8dp"
        android:left="10dp"
        android:right="10dp"
        android:top="8dp" />
</shape>